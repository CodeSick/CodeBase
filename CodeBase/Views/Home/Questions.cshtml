@model IEnumerable<CodeBase.ViewModel.QuestionsWithCount>
<table>
    <tr>
        <th>
            Popular Questions
        </th>
        <th>
            Answers
        </th>
    </tr>
@foreach (var item in Model) {
    <tr>
        <td>
            @(Html.ActionLink<QuestionsController>(x => x.Details(item.Question.QuestionId),item.Question.Title)) 
        </td>
        <td>
            @item.Count
        </td>
    </tr>
}

</table>